events {}
http {
    upstream load-balancing {
        server swarm-manager:5000;
        server swarm-worker:5000;
    }
    server {
        location / {
            proxy_pass load-balancing;
        }
    }
}